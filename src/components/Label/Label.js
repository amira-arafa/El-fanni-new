import React from "react";

const Label = ({ title }) => {
  return (
    <label className="inter-semi-bold label-1 mb-0 low-line-height d-block">
      {title}
    </label>
  );
};

export default Label;
